pipeline {
    agent any

    stages {
        stage('Checkout Verification') {
            steps {
                echo 'Pipeline triggered successfully'
                sh 'ls -la'
            }
        }
    }
}

